import React from 'react'
import Directory from '../../cmponents/Directory'
import "./style.scss"

const Homepage = (props) => {
    return (
        <section className='homepage'>
            <Directory/>
            
        </section>
    )
}

export default Homepage
